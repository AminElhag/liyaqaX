# STAGING PROFILE — matches prod except observability is on
spring:
  flyway:
    clean-disabled: true
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: WARN
    com.liyaqa: INFO
