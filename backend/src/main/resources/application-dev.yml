# DEV PROFILE — never use these settings in staging or production
spring:
  flyway:
    clean-disabled: false
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    com.arena: DEBUG
