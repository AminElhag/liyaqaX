package com.liyaqa

import org.junit.jupiter.api.Test

class LiyaqaApplicationTest {
    @Test
    fun contextLoads() {
        // Spring context load verification
        // No assertions needed — if context fails to load, this test fails
    }
}
